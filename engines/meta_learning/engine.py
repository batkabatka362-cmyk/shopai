"""
MetaLearning Engine — Learn from learning — optimize learning strategies, transfer knowledge across domains
"""
from __future__ import annotations
from typing import Any
from engines.base import BaseEngine, EngineStep, StepResult, EngineStatus
from models.routing.model_router import ModelRouter


class MetaLearningEngine(BaseEngine):
    engine_name = "meta_learning"
    required_input_fields = ['learning_data', 'performance_history']
    required_output_fields = ['meta_insights', 'improvement_plan']

    def __init__(self) -> None:
        self._model_router = ModelRouter()
        super().__init__()

    def define_steps(self) -> None:
        self.flow.add_step(EngineStep(name="analyze", model_role="analyzer", description="Domain analysis", required=True, stop_on_reject=True))
        self.flow.register_executor("analyze", self._step_analyze)
        self.flow.add_step(EngineStep(name="execute", model_role="worker", description="Generate structured output", required=True))
        self.flow.register_executor("execute", self._step_execute)
        self.flow.add_step(EngineStep(name="enhance", model_role="creative", description="Creative enhancement", required=False))
        self.flow.register_executor("enhance", self._step_enhance)
        self.flow.add_step(EngineStep(name="validate", model_role="validator", description="Quality validation", required=True))
        self.flow.register_executor("validate", self._step_validate)

    def _step_analyze(self, step_name: str, data: dict[str, Any]) -> StepResult:
        r = self._model_router.execute("analyzer", self._build_prompt("analyze", data), context=data)
        return StepResult(step_name=step_name, model_used="mistral", status=EngineStatus.COMPLETED, output={"analysis": r})

    def _step_execute(self, step_name: str, data: dict[str, Any]) -> StepResult:
        r = self._model_router.execute("worker", self._build_prompt("execute", data), context=data)
        return StepResult(step_name=step_name, model_used="qwen", status=EngineStatus.COMPLETED, output={"execution": r})

    def _step_enhance(self, step_name: str, data: dict[str, Any]) -> StepResult:
        r = self._model_router.execute("creative", self._build_prompt("enhance", data), context=data)
        return StepResult(step_name=step_name, model_used="llama", status=EngineStatus.COMPLETED, output={"enhanced": r})

    def _step_validate(self, step_name: str, data: dict[str, Any]) -> StepResult:
        r = self._model_router.execute("validator", self._build_prompt("validate", data), context=data)
        return StepResult(step_name=step_name, model_used="mistral", status=EngineStatus.COMPLETED, output={"validation": r})

    def _build_prompt(self, step: str, data: dict[str, Any]) -> str:
        templates = {"analyze": """Analyze learning performance: which engines improve fastest, knowledge transfer opportunities, learning plateaus, diminishing returns detection.\nLearning: {learning_data}\nHistory: {performance_history}""", "execute": """Generate meta-learning plan: learning strategy per engine, knowledge transfer priorities, curriculum design, feedback loop optimization.\nAnalysis: {analysis}""", "enhance": """Enhance: learning acceleration techniques, compounding knowledge, emergent capabilities.\nPlan: {execution}""", "validate": """Validate: insights are actionable, improvements measurable, no overfitting to past.\nOutput: {enhanced}"""}
        t = templates.get(step, "")
        try:
            return t.format(**data)
        except KeyError:
            return t + "\nData: " + str(data)

    @staticmethod
    def _learning_curve_position(accuracy: float, max_accuracy: float) -> str:
        ratio = accuracy / max(max_accuracy, 0.01)
        if ratio < 0.3: return "early"
        if ratio < 0.7: return "rapid_improvement"
        if ratio < 0.9: return "approaching_plateau"
        return "plateau"

    @staticmethod
    def _transfer_potential(source_accuracy: float, domain_similarity: float) -> float:
        return round(source_accuracy * domain_similarity, 3)
